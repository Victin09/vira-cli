import * as React from 'react';

const Home = (): JSX.Element => (
    <div className="h-100 d-flex justify-content-center align-items-center">React app template with Bootstrap!</div>
);

export default Home;
